import os

max_requests = 500  # totally arbitrary numbers, need adjustments
max_requests_jitter = 150  # totally arbitrary numbers, need adjustments
workers = 3  # totally arbitrary numbers, need adjustments
worker_class = "gevent"
